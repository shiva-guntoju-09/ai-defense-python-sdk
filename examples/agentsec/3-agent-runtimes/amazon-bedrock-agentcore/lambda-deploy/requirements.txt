# Requirements for AWS Lambda Deploy
# Note: cisco-aidefense-sdk (aidefense/) is copied from source (not available on PyPI)

strands-agents>=0.1.0
python-dotenv>=1.0.0
httpx>=0.27.0
requests>=2.28.0

# SDK dependencies (since aidefense is copied as source, not pip installed)
wrapt>=1.14.0
pyyaml>=6.0
aiohttp>=3.9.0
pydantic>=2.0.0
tenacity>=9.0.0
